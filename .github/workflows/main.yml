name: Generate a deplyment file
on: [push]
jobs:
 build:
  runs-on: ubuntu-latest
  steps: 
   - name: Checkout Repo
    uses: actions/checkout@v3
  - name: Setup Python
    uses: actions/setup-python@v4
    with:
     python-version: '3.10'
- name: Install Dependencies
 run: 
 python -m pip install --upgrade pip
 pip install pyyaml
- name:  Run Python File
 run: python odoo-dev.conf
 - name: Push Repo
 run:
 git config user.name github-actions
 git config user.email salmanhabib11186@gmail.com
 git add .
 git commit -m "Modified Feed"
 git push
 
 
  
